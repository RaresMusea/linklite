apiVersion: v1
kind: Service
metadata:
  name: linklite-app
  namespace: linklite-preprod
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: linklite
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: http
